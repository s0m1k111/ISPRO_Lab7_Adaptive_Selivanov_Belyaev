# Лабораторная работа №7: Адаптивная вёрстка (Flexbox)

**Студент:** Селиванов Павел Алексеевич  
**Группа:** ИСП-233  
**Дата выполнения:** 02.02.2026

---

## 🎯 Цель работы

Освоить современные технологии CSS-разметки — **Flexbox**, научиться создавать адаптивные макеты.

---

## 📋 Задачи

1. Изучить основы Flexbox.
2. Практически отработать свойства:
   - `flex-direction`
   - `justify-content`
   - `align-items`
   - `flex-wrap`
   - `gap`
   - `flex-grow`, `flex-shrink`, `flex-basis`
3. Создать практические примеры: навигационное меню, карточки товаров, центрирование элементов.

---

## Выполнение работы

### 1. Базовая структура

В ходе работы были созданы следующие файлы:

- `index.html` — основная страница для экспериментов с Flexbox.
- `styles.css` — стили для базовых примеров.
- `flexbox.html` и `flexbox.css` — практические примеры.

### 2. Изученные свойства Flexbox

#### `flex-direction`

- `row` (по умолчанию) — элементы в строку.
- `column` — элементы в столбец.
- `row-reverse` — обратный порядок строки.
- `column-reverse` — обратный порядок столбца.

#### `justify-content`

- `flex-start`, `flex-end`, `center`.
- `space-between`, `space-around`, `space-evenly`.

#### `align-items`

- `stretch`, `flex-start`, `flex-end`, `center`, `baseline`.

#### `flex-wrap`

- `nowrap` (по умолчанию).
- `wrap` — перенос на новую строку.
- `wrap-reverse` — обратный перенос.

#### `gap`

- Современная замена `margin` для отступов между элементами.

---

## Практические примеры

### Пример 1: Навигационное меню

```css
.navbar {
  display: flex;
  justify-content: space-around;
  background-color: #333;
  padding: 15px;
}
```

### Пример 2: Карточки товаров

```css
.cards {
  display: flex;
  gap: 20px;
  flex-wrap: wrap;
}

.card {
  flex: 1 1 250px;
}
```

### Пример 3: Центрирование элемента

```css
.centered {
  display: flex;
  justify-content: center;
  align-items: center;
  height: 200px;
}
```
